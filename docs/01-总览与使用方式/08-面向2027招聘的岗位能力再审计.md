# 面向 2027 招聘的岗位能力再审计

> 样本核验日期：2026-08-31｜目标：西安 AI Agent 应用开发工程师 / AI 应用开发工程师｜下一次强制复核：2026-11-01。

## 1. 为什么需要再次反推课程

“会 LangChain”不是岗位能力，它只是实现路径之一。招聘方真正购买的是：把不稳定的模型能力接入真实业务，并将其做成可交付、可排障、可评测、可迭代的系统。

这一轮不以技术热度决定课程，而采用以下证据链：

```text
当前 JD 职责与要求
  → 岗位族分类
  → 能力出现强度与淘汰风险
  → 教程章节
  → 项目产出
  → 简历证据
  → 面试追问与现场题
```

公开 JD 会过期、会被搜索引擎截断，也可能存在招聘平台转录误差。因此样本只用于发现共同能力，不用于推断岗位数量、薪资或录用概率；投递前必须重新采样。

## 2. 2026-08 岗位样本观察

本轮优先查看西安岗位，再使用全国企业岗位校验工程趋势。样本并不等权：公司官网岗位可信度高于聚合页；算法岗只用于识别岗位边界，不能反向绑架应用开发主线。

| 样本 | 地点/方向 | 明确出现的能力 | 对课程的含义 |
| --- | --- | --- | --- |
| 瑞风协同 AI 应用开发（Python/Java） | 西安，3～5 年 | Python/FastAPI、REST、LangChain/LangGraph、RAG、MCP、MySQL/PostgreSQL、Redis、系统设计 | 与当前转型目标最贴近，Python 后端与数据库必须达到工程水平 |
| 神州信息 AI Agent 工程师 | 西安 | Python、Agent/RAG、异步、REST、分布式任务调度、Redis/MQ、交付协作 | 需要补任务队列、消息语义和中间件，不只是请求内 Agent Loop |
| 北京计算机技术及应用研究所智能体开发 | 北京/西安 | LangChain、RAG、MCP、Skills、Dify、FastAPI、部署运维、需求与技术文档 | 需要具备代码框架与低代码平台双视角，以及从需求到上线的证据 |
| 普联软件 AI 智能体开发 | 多地含西安 | Dify/Coze、Python、模型使用、服务部署、文档和项目验收 | 企业交付岗位会考平台使用、沟通和验收，而非只考算法 |
| 中科院西安光机所 Agent 方向 | 西安，研究/算法倾向 | Transformer、PyTorch/Hugging Face、RAG、微调、LangGraph、部署 | 模型原理与微调边界要能解释，但其学历与算法深度不是本课程主投方向 |
| 华为云 AI 应用研发 | 多地含西安，平台/Infra 倾向 | 后端、Docker/K8s、vLLM/SGLang、任务调度、服务治理 | 云原生与推理服务要懂边界；不把 GPU 调度和推理引擎研发设为必修 |
| 百度智能体平台方向 | 北京，高级岗位 | 企业 Agent 平台、Dify/LangChain、RAG、向量检索、评测与优化 | “平台化、评测、稳定性”是高级应用岗位的演进方向 |
| 贵州茅台 AI 工程师 | 全国企业样本 | Python/FastAPI、向量库、MQ、Fine-tuning/LoRA、Docker/K8s、DevOps、多模态 | 验证企业岗位常把应用、部署和算法加分项写在同一 JD，必须会判断主次 |

### 2.1 高频共同项

按是否会直接影响简历筛选和技术面试，将能力重新分层。

**P0：2027-02 前必须能用项目证明**

1. Python：类型、异步、异常、测试、包管理、代码组织和基本性能分析；
2. FastAPI/REST/SSE：认证、错误契约、超时、并发、流式断线和接口测试；
3. LLM 应用基础：Prompt、Context、Structured Output、Function Calling、模型选型；
4. LangChain 与 LangGraph：能写、能调、能解释何时不用；
5. RAG：摄取、切分、向量/混合检索、重排、引用、拒答和分层评测；
6. Agent 工程：状态、记忆、审批、幂等、停止条件、任务恢复和权限；
7. 数据与中间件：SQL、事务、索引、Redis、后台任务/队列的交付语义；
8. MCP 与外部系统集成：工具协议、OAuth、Java/第三方 API 边界；
9. 生产质量：日志/指标/Trace、限流、降级、负载测试、安全和 CI；
10. 独立交付：需求澄清、技术方案、开发测试、上线检查、问题定位和文档。

**P1：目标 JD 出现时应能完成小实验或演示**

- Dify 工作流：能把同一业务分别映射到可视化节点和 LangGraph，并说明迁移边界；
- Docker/Kubernetes：能写构建和部署配置，理解 Pod/Deployment/Service/Probe，不要求本机维护集群；
- 模型部署：理解 vLLM 的 OpenAI-compatible 服务、并发/批处理/KV Cache/量化的关系；
- PyTorch/Hugging Face：能读懂张量、Tokenizer、Causal LM 和 PEFT 示例；
- 多模态/OCR：能把图片或扫描文档接入受控处理链，而不是训练视觉模型；
- 开源协作：至少完成一个高质量 Issue、文档 PR 或可复现 Bug 报告。

**P2：面试级理解，除非 JD 明确要求才深入**

- SFT/LoRA 数据生产和训练实操；
- vLLM/SGLang/TensorRT-LLM 性能调优；
- Kubernetes 调度器、GPU 集群和分布式训练；
- RLHF/DPO、预训练、CUDA 算子和模型结构研究；
- GraphRAG、Text2SQL、多模态训练等垂直专题。

## 3. 三类岗位必须分开判断

### 3.1 AI 应用 / Agent 应用开发：本课程主投

典型职责是接模型、建 RAG/Agent、集成业务 API、交付后端服务、评测优化和上线排障。面试权重大致落在：项目深挖、Python 后端、Agent/RAG 原理、系统设计和业务取舍。

你现有四年前端经验可以证明软件交付、接口协作和用户体验，但不能自动证明 Python 后端深度。需要用项目中的异步、数据库、队列、权限、测试和故障复盘补足。

### 3.2 Agent 平台 / AI 平台工程：可投但需 JD 适配

除了应用能力，还会要求租户、配额、模型网关、工作流平台、插件生态、部署与可观测性。主项目完成后已具备基础，但遇到强 K8s、模型托管或平台控制面要求时，要降低匹配分而不是硬投。

### 3.3 大模型算法 / 推理 Infra：不是当前主线

这类岗位常要求研究生背景、PyTorch 深度、训练、量化、GPU、CUDA 或推理引擎。能解释 Transformer、LoRA、vLLM 并不等于具备算法/Infra 岗能力。不要在简历中把“调用云 API”写成“模型部署优化”。

## 4. 课程缺口与本轮补充决策

| 发现的缺口 | 若不补的面试风险 | 补充位置 | 目标证据 |
| --- | --- | --- | --- |
| Redis、MQ、分布式任务只有零散名词 | 无法设计文档摄取、长任务和失败重投 | Python 第 7 章 | 可恢复摄取 Job、幂等测试、DLQ 设计 |
| Transformer/LoRA/vLLM 只列为 P2 | 面试无法说明模型能力、延迟和选型来源 | LLM 第 7 章 | 模型选型实验和训练/推理边界 ADR |
| Dify 在 JD 高频出现但教程只有一个关键词 | 平台型岗位无法说明节点、DSL、代码迁移和治理 | LangChain 第 8 章 | 同一流程的 Dify/LangGraph 对照图 |
| Linux/Docker 章节过短 | 不会从 502、SSE 缓冲、OOM、FD、DNS 等现象排障 | 工程化第 6 章深化 | 部署清单、故障演练和诊断记录 |
| 计算机基础只有答案摘要 | Python/SQL/网络现场追问容易失分 | 面试第 5 章深化 | 12 周训练记录、SQL 与代码题提交 |
| 缺从需求到上线的交付评审 | 项目像个人 Demo，无法证明企业协作 | 企业项目第 9 章 | RFC、发布计划、验收纪要、回滚演练 |
| 缺岗位到证据的最终门禁 | “学过很多”但简历没有可信证据 | 面试第 8 章 | JD 匹配表、证据索引和投递门槛 |

## 5. 不增加第 25 周的时间安排

本轮补充不是再堆一套技术栈，而是替换原先过于概括的阅读时间。

| 内容 | 安排 | 时间 |
| --- | --- | ---: |
| 岗位再审计与证据矩阵 | 第 1 周建立；第 10、18、23 周复核 | 4 次 × 45 分钟 |
| Transformer、微调与推理边界 | 第 5 周 | 2 小时，替换泛化原理阅读 |
| Redis 与可靠后台任务 | 第 4 周建接口，第 11 周用于文档摄取，第 20 周故障演练 | 共 5 小时，来自对应项目编码 |
| Dify 对照实验 | 第 10 周 | 2 小时，不单独重做业务逻辑 |
| Linux/部署排障 | 第 22～23 周 | 4 小时，替换重复部署阅读 |
| 基础题训练 | 24 周每周 2 小时 | 使用既有“横向基础”时间 |
| 交付评审 | 第 23～24 周 | 使用项目收口时间 |

如果进度落后，先取消 Dify 实机操作和云模型部署，只保留架构对照；不得削减 Python、RAG、LangGraph、数据库、队列语义、测试和项目表达。

## 6. 面试胜任标准

“教程读完”不是胜任。投递前应同时满足：

- 给定一个业务问题，30 分钟内完成需求澄清、核心状态、工具与风险设计；
- 不查教程写出 FastAPI 接口、Pydantic Schema、异步 Tool 调用和错误映射；
- 从空库建立文档摄取、检索、引用和拒答的 RAG 最小闭环；
- 使用 LangGraph 实现持久化、Interrupt、恢复和幂等副作用；
- 解释 Redis 缓存与任务队列的不同，以及至少一次投递为何仍需幂等；
- 根据 Trace/Metric/Log 定位一个真实故障，而不是只会添加日志；
- 用测试和评测数据证明优化有效，并说明样本限制；
- 展示一个失败案例、一个架构取舍和一个上线回滚方案；
- 对陌生 JD 能诚实区分“已证明、理解、未掌握”。

## 7. 练习与答案

### 练习 1：JD 同时写 LangChain、Dify、微调、K8s，是否四项都要精通？

**答案：**先判断岗位族和职责主线。若职责是企业知识库、Agent 工作流和 FastAPI 交付，LangChain/RAG/Python 是 P0，Dify 是 P1，微调/K8s 多半是加分或团队协作边界。若职责是训练或 GPU 平台，则属于另一岗位族。用实际职责、学历、年限和项目描述共同判断，不能只数关键词。

### 练习 2：四年前端经验能否直接满足“3 年以上 Python/Java 开发经验”？

**答案：**不能直接等同。可以诚实写“四年软件工程与前端交付经验”，再用 Git 历史、测试、架构文档和可运行项目证明 Python Agent 后端能力。对明确要求三年 Python 商业经验的岗位降低匹配分，不伪造年限。

### 练习 3：为什么不把 LoRA 训练设为主项目必修？

**答案：**目标岗位是应用开发，当前设备也不适合本地训练。主项目更需要 RAG、Tool、状态、评测、安全和交付深度。课程仍补齐 Transformer、SFT/LoRA、量化和服务边界，以便面试选型；只有目标 JD 高频且明确要求训练实操时，再使用云端小模型实验升级。

### 练习 4：如何证明自己不是只会做 Demo？

**答案：**展示可重复测试、离线评测集、权限负向用例、迁移文件、队列失败重投、SLO、负载报告、Runbook、发布回滚和事故复盘。业务功能只是起点，失败时仍可控才是企业证据。

## 8. 验收清单

- [ ] 保存至少 20 条带日期和原链接的目标 JD，不只保存截图；
- [ ] 每条 JD 标注岗位族、P0/P1/P2、硬门槛和项目证据；
- [ ] 不把算法岗或 AI Infra 岗的要求错误加入应用开发必修；
- [ ] 为出现率高但无证据的能力创建 48 小时补缺任务；
- [ ] 第 18 周后至少用 3 条真实 JD 反向审查项目 README；
- [ ] 投递前重新核验岗位是否仍在、职责是否变化。

## 9. 岗位资料来源

- [瑞风协同：AI 应用开发工程师（Python/Java）](https://www.zhaopin.com/jobdetail/CC388480710J40822509716.htm)
- [神州信息：AI Agent 工程师（西安）](https://www.nowcoder.com/jobs/detail/437310)
- [北京计算机技术及应用研究所：大模型/智能体开发（北京/西安）](https://bebee.com/cn/jobs/2026--techmap_cn_CC120834970J40934435702)
- [普联软件：AI 智能体开发](https://www.zhaopin.com/jobdetail/CC153424410J40706678416.htm)
- [中国科学院西安光机所 2026 招聘](https://www.gaoxiaojob.com/announcement/detail/413414.html)
- [华为云算法创新 Lab 招聘](https://www.huaweicloud.com/lab/algorithm/campus_recruitment.html)
- [百度：高级算法工程师（智能体平台方向）](https://talent.baidu.com/jobs/detail/SOCIAL/44e2693e-7f8b-47bc-82d7-de0870f59d2b)
- [贵州茅台 2026 社招岗位表](https://www.moutaichina.com/mtgf/2026-04/24/25dae6b7b53d4a6682482f764eba595d/2026042418071183663.pdf)
