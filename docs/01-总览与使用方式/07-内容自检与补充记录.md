# 教程内容自检与补充记录

> 最近自检：2026-08-31｜用途：让教程的“完整”可以被检查，而不是只由文件数量证明。

## 1. 自检标准

每篇核心技术教程至少满足：

1. 学习目标和预计投入明确；
2. 解释核心心智模型与适用边界；
3. 提供可运行代码、协议示例或足够具体的伪代码；
4. 说明至少一个失败路径、安全边界或版本陷阱；
5. 能并入“智维 Agent”主项目；
6. 至少两道练习并紧跟答案；
7. 有可验证的验收标准；
8. 有对应官方资料；
9. 框架 API 标明版本基线，不混用旧接口；
10. 不把 Prompt、框架默认值或“模型自觉”当作企业安全与可靠性保证。

教程整体还要满足：阶段里程碑一致、24 周总时间可执行、前端内容不重复、Python Agent 主线不被 Java 增强线打断、本地无 Docker/本地模型也能学习。

## 2. 本轮发现与修复

| 优先级 | 发现 | 风险 | 修复 |
| --- | --- | --- | --- |
| P0 | M0～M6 章节周数与 24 周计划错位 | 学习顺序和验收混乱 | 统一为第 4/7/10/14/18/19/22 周 |
| P0 | 环境示例仍保留月度预算变量 | 与“API 金额无上限”约定冲突 | 改为费用异常告警开关 |
| P0 | MCP 只有本地只读 Server，缺远程 Transport/Auth | 无法解释企业远程 MCP | 新增 Streamable HTTP、OAuth、Session 和安全章节 |
| P0 | 没有 Python 数据层/迁移教程 | 只能 CRUD，无法可靠保存运行与审批记录 | 新增 SQLAlchemy 2、AsyncSession、Alembic 与事务边界章节 |
| P0 | RBAC、多租户只散落在各章 | 容易把 Prompt/Tool 过滤误当授权 | 新增身份、Scope、资源所有权和跨租户测试章节 |
| P1 | LangSmith 无法覆盖 HTTP/DB/跨服务生产观测 | 出现性能问题时无法定位 | 新增 OpenTelemetry、Metric、SLO、k6 与负载测试章节 |
| P1 | LangChain 章节缺当前 Runtime/Structured Output 代码 | 看懂概念但无法落地 | 补充 `context_schema`、`ToolRuntime`、`ToolStrategy` 和错误契约 |
| P1 | Middleware 顺序和多层重试未说明 | 可能泄密或放大请求 | 补充 Hook 顺序、嵌套语义和唯一重试所有者 |
| P1 | Streaming 只讲概念 | 前端协议与框架事件容易耦合 | 补充 `updates/messages/custom`、v2 适配与终态契约 |
| P1 | LangGraph 并行缺 `Send`、Pending Writes 细节 | 无法回答 Map-Reduce 与恢复追问 | 补充 `Send`、Super-step、持久化与并发恢复策略 |
| P1 | Interrupt 缺完整恢复代码 | 容易把聊天“确认”当可信审批 | 补充 `interrupt`、`Command(resume)`、动作摘要与重校验 |
| P1 | RAG M3 只有验收条目，没有端到端实现闭环 | 会调组件但不会定位失败 | 补充数据契约、摄取、RRF、引用校验、分层评测和故障表 |
| P1 | LangGraph M4/M5 只有清单，没有综合代码与 CI 实验 | 无法形成可展示证据 | 补充 Graph 骨架、恢复矩阵、LangSmith Dataset/Evaluator/Experiment 与发布门禁 |
| P2 | M5 资料链接有一处过期 | 学习入口失效 | 替换为当前 LangSmith pytest 评测页 |

## 3. 内容覆盖矩阵

| 能力 | 教程位置 | 可验证产出 |
| --- | --- | --- |
| Python 类型、异步、API、测试 | `02-Python-Agent后端基础` | M0 模型网关 |
| 原生 Tool Loop | `03-LLM应用与原生Agent` | M1 原生 Agent |
| LangChain 1.x | `04-LangChain与单Agent` | M2 设备工具助手 |
| RAG | `05-RAG与知识系统` | M3 维护知识助手 |
| LangGraph 1.x | `06-LangGraph有状态工作流` | M4 工单协同 Graph |
| LangSmith 与 Eval | `07-LangSmith评测与可观测性` | M5 自动回归报告 |
| MCP、安全、数据、可观测性 | `08-MCP安全与工程化` | M6 工程验收 |
| Java 业务边界 | `09-Java业务服务集成` | 可插拔业务服务 |
| 交付与面试 | `10`、`11` | Release Candidate 与面试证据 |

## 4. 版本核验结论

本轮按 2026-08-31 官方资料核验以下内容：

- LangChain 使用 1.x `create_agent`，Structured Output 使用 `ProviderStrategy/ToolStrategy`；Runtime Context 通过 `context_schema` 注入。
- LangChain Streaming 包含 `updates/messages/custom`；v2 统一格式要求锁定支持该功能的 LangGraph 版本。
- LangGraph 持久化围绕 Checkpoint、Thread、Pending Writes；Interrupt 恢复使用 `Command(resume=...)`。
- LangGraph 动态 Map-Reduce 使用 `Send`，并行分支属于 Super-step，更新顺序不能当业务排序。
- MCP 正式学习基线固定为 2025-11-25；远程标准 Transport 是 Streamable HTTP，旧 HTTP+SSE 只作为兼容知识。
- MCP HTTP Authorization 基于 OAuth 体系、Protected Resource Metadata 和目标 Resource/Audience 绑定；stdio 不照搬 HTTP OAuth 流程。
- SQLAlchemy 使用 2.x `select()` 与每并发 Task 独立 `AsyncSession`；Schema 变更由 Alembic Migration 管理。
- OpenTelemetry Python 的 Trace 与 Metric 可用于课程生产观测；Logs 的成熟度和具体 Exporter 仍以进入该阶段时的官方状态为准。

## 5. 进入每个阶段前的二次检查

框架和模型会继续更新，因此“本轮通过”不等于 2027 年无需复核。进入 LangChain、LangGraph、MCP 阶段的第一天执行：

```text
1. 查看 uv.lock 中真实版本
2. 打开对应官方 Versioning/Migration/Specification
3. 跑该模块最小导入与 Smoke Test
4. 核对教程代码签名
5. 更新版本矩阵和 ADR
6. 跑上一里程碑回归
```

API 发生变化时优先修改 Adapter 和教程示例，不改变稳定的业务边界、评测数据与外部 SSE/API 契约。

## 6. 学习者自检题与答案

### 自检 1：一篇教程有概念、练习和官方链接，但没有可运行产出，算完成吗？

**答案：**不算。至少要能用代码、测试、协议样例或评测结果证明理解，并并入主项目。

### 自检 2：官方文档中的 Demo 可以直接视为企业架构吗？

**答案：**不能。Demo 用于说明 API；身份、租户、幂等、事务、数据保留、错误契约、可观测性和部署由应用架构补齐。

### 自检 3：为什么教程还要在进入阶段前复核版本？

**答案：**LangChain、LangGraph、MCP 和模型能力变化快。长期教程应固定稳定概念和验证方法，代码签名则以锁定版本及对应官方文档为准。

## 7. 本轮验收结果

- 新增四篇企业工程补缺教程，每篇包含练习、答案、验收与官方资料；
- LangChain 与 LangGraph 关键章节已经补入当前 API 代码和失败边界；
- RAG M3、LangGraph M4、LangSmith M5 已补成端到端综合实验与可提交清单；
- M0～M6 周数与 24 周计划一致；
- 固定 API 金额预算变量已移除，成本只用于观测与企业决策；
- MkDocs 严格构建、内部链接、章节结构和线上部署需在每次修改后自动检查。

## 8. 资料来源

- [LangChain Agents](https://docs.langchain.com/oss/python/langchain/agents)
- [LangGraph Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)
- [MCP 2025-11-25 Specification](https://modelcontextprotocol.io/specification/2025-11-25/)
- [SQLAlchemy 2.0 Documentation](https://docs.sqlalchemy.org/en/20/)
- [OpenTelemetry Python](https://opentelemetry.io/docs/languages/python/)
