# 贡献指南

感谢参与完善这套 Agent 学习教程。

## 贡献流程

1. Fork 仓库并从最新 `main` 创建功能分支。
2. 修改 Markdown；新增技术结论时优先引用官方文档、规范或论文。
3. 每篇技术教程保留学习目标、项目任务、练习与答案、验收和资料来源。
4. 本地运行 `mkdocs build --strict`，确保链接和站点构建没有警告。
5. 提交 Pull Request，说明修改动机、影响章节、验证方式和资料来源。
6. 等待 `docs-build` 检查通过并由 `@zhanghuaiwei` 审核。
7. 审核通过并合并到 `main` 后，站点自动部署；PR 本身不会发布。

## 分支与审核规则

- 禁止协作者直接向 `main` 推送内容变更。
- 至少需要 1 个批准审核。
- 新提交会使旧批准失效，需重新审核最新内容。
- 所有 Review Conversation 必须解决。
- `docs-build` 必须通过。
- `CODEOWNERS` 指定 `@zhanghuaiwei` 为全仓库内容所有者。

## 内容标准

- 使用简体中文，技术名词、API 和代码保持准确英文。
- 不复制大段受版权保护内容；以自己的语言讲解并链接来源。
- 示例不得包含真实 API Key、个人隐私或企业机密。
- 不以复杂度为目标；新增框架或模式需说明适用边界与代价。
- 改动 LangChain、LangGraph、LangSmith 或 MCP 内容时注明验证版本或日期。

## Pull Request 描述模板

```markdown
## 修改目的

## 影响章节

## 验证
- [ ] `mkdocs build --strict` 通过
- [ ] 练习和答案同步更新
- [ ] 官方资料链接已核验
- [ ] 不包含 Secret 或敏感数据

## 兼容性与迁移说明
```

